import 'package:flutter/material.dart';

class DisasterScreen extends StatelessWidget {
  const DisasterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Disaster Page'),);
  }
}